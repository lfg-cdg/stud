Задание 1

[] == [] - true

[] is [] - true

a = []; b = a; a is b - true

a = [1]; b = [1]; a == b - true

a = [1]; b = [1]; a is b - false

None is None - true

None == None - true

a = 256; b = 256; a is b (важно: ответ с пояснением “почему нельзя полагаться”) - true вроде как потому что кешируется до 256 и одинаковая ячейка в памяти используется для этих чисел

a = "hello"; b = "hello"; a == b - true

a = "hello"; b = "hello"; a is b (аналогично — с пояснением) - false т.к 2 разные переменные занимают 2 разные ячейки памяти и ссылаются на разные объекты